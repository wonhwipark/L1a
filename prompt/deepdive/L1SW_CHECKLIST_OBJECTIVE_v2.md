# L1SW 확인 체크리스트 v2 — 객관식 (현장용)

> ✅ **사용 완료 (2026-06-20 온사이트 확인).** 응답 결과는
> `L1SW_AND_FLOW_20260620.md`에 반영됨. 빈 `응답:` 란은 미완성이 아니라
> 현장 조사용 원본 양식이며 이력 보존용으로 유지한다.

**작성:** 2026-06-21 KST
**용도:** 사내 Claude Code에서 L1SW 스킬 파일을 읽으며 번호만 표기
**배경:** `L1SW_VERIFICATION_QUESTIONS_v2.md` (서술형) 참조
**소요:** 15~20분

> 사용법: 각 질문에서 해당 번호를 "응답:" 뒤에 적는다.
> "기타:" 는 선택지에 없는 경우에만 자유 기입.
> 각 질문 아래 `# →` 주석은 답변에 따른 5.5 설계 영향.

═══════════════════════════════════════════════════
## A. 기존 미확인 항목 (재확인)
═══════════════════════════════════════════════════

A1. _l1sw.txt 크기 축소율 (.sdm 대비)
  1. ~80% 축소 (이전 확인과 동일)
  2. ~90% 이상 축소
  3. ~50~70% 축소
  4. 케이스마다 편차 큼 (범위:         )
  5. 기타:

응답:

# → 3이면: _l1sw.txt 위 2차 grep의 중요도 상승
# → 1·2면: _l1sw.txt 자체로 충분히 작아 2차 grep은 선택적

---

A2. -TimeFrom/-TimeTo + -Modules 적용 후에도 Context 초과 발생하는가?
  1. 거의 없음 (대부분 Context 이내)
  2. 가끔 발생 (대략     MB /     만 줄 수준에서)
  3. 자주 발생
  4. 기타:

응답:

# → 2·3이면: 5.5 pre-filter 2차 grep이 필수
# → 1이면: 2차 grep 없이 _l1sw.txt 직접 분석 가능할 수 있음

---

A3. 과거 HTML 리포트를 다음 분석에서 참조/재사용하는 메커니즘이 있는가?
  1. 없음 (매번 독립 분석)
  2. @mention으로 수동 참조 가능
  3. 자동으로 이전 결과를 불러오는 기능 있음
  4. 기타:

응답:

# → 1이면: 5.5 cases/ 누적이 유일한 이력 관리 수단 (가치 높음)
# → 2·3이면: 5.5는 L1SW 이력과 연계 방식 설계 필요

---

A4. issue_type(rach_failure, scg_failure 등) 기반 분류 체계가 L1SW에 있는가?
  1. 없음 — 모듈명 기반 분류만 존재
  2. 있음 — issue_type 분류 체계 (구조:              )
  3. 일부만 (예:              )
  4. 기타:

응답:

# → 1이면: issue_type 분류는 온전히 5.5 고유 가치
# → 2·3이면: 5.5 taxonomy와 정합 필요

---

A5. 분석 결과를 HTML 외에 구조화 데이터(YAML/JSON/CSV)로 병행 저장하는가?
  1. 없음 — HTML only
  2. 있음 — 형식:
  3. 기타:

응답:

# → 1이면: 5.5 Case YAML이 유일한 구조화 저장소
# → 2이면: 5.5 Case YAML과 L1SW 출력 간 필드 매핑 필요

═══════════════════════════════════════════════════
## B. 키워드/필터링 구조 상세 (5.5 keywords.yaml 설계용)
═══════════════════════════════════════════════════

B1. fragment JSON의 키 구조는?
  1. 키 = 모듈명, value = regex 1개
  2. 키 = 모듈명, value = regex 배열 (여러 개)
  3. 키 = 키워드/패턴, value = 모듈 매핑
  4. 위와 다른 구조 (설명:                      )
  5. 기타:

응답:

# B1 추가: regex 복잡도는?
#   a. 단순 문자열 매칭 (fixed string)
#   b. 복합 정규식 (그룹, OR, 수량자 등)
#   c. 혼재

응답:

# → 1·2면: 모듈→regex 축이라 issue_type 매핑은 5.5가 추가해야 함
# → 3이면: L1SW가 이미 키워드 단위로 관리 → 5.5 keywords.yaml과 겹칠 수 있음

---

B2. 5개 fragment JSON은 어떤 기준으로 나뉘어 있는가?
  1. 모듈 그룹별 (예: L1계열, L2계열 등)
  2. 계층별 (L1/L2/RRC/NAS 등)
  3. 기능별 (RACH/측정/스케줄링 등)
  4. 다른 기준 (설명:                      )
  5. 기타:

응답:

# → 3이면: issue_type과 이미 겹치는 구조 → keywords.yaml은 보완 역할
# → 1·2면: 모듈/계층 축이라 issue_type 축은 5.5 고유

---

B3. ★ fragment JSON 키워드 중 특정 issue_type에 직접 대응하는 항목이 있는가?
  1. 없음 — 모듈 단위로만 정의, issue_type 매핑은 사용자 머릿속
  2. 일부 있음 (예:                      )
  3. 있음 — issue_type별로 명시적 매핑 존재
  4. 기타:

응답:

# ★ 핵심 질문 — 5.5 keywords.yaml의 SSOT 여부 결정
# → 1이면: keywords.yaml이 issue_type↔키워드 매핑의 유일한 SSOT
# → 2·3이면: keywords.yaml은 L1SW 매핑을 확장/보완하는 역할로 재정의

---

B4. ★ -Modules 옵션만으로 issue_type별 분석을 근사할 수 있는가?

rach_failure 분석 시 넣는 모듈:
scg_failure 분석 시 넣는 모듈:
tx_abnormal 분석 시 넣는 모듈:
l2_max_retransmission 분석 시 넣는 모듈:

결과 중 해당 issue_type과 직접 관련된 비율은?
  1. 대부분 관련 (80%+) — 잡음 적음
  2. 절반 정도 (40~70%) — 잡음 있지만 쓸만
  3. 관련 비율 낮음 (~30% 이하) — 잡음 많음
  4. 모듈 조합으로 근사 자체가 어려움
  5. 기타:

응답:

# → 1이면: _l1sw.txt에서 추가 grep 불필요할 수 있음
# → 2·3·4면: 5.5 pre-filter 2차 grep이 필수 (현재 설계 유지)

---

B5. _l1sw.txt 로그 라인의 실제 포맷은?

예시 라인 2~3줄 (복사해서 붙여넣기):
```


```

포맷 구성 요소 확인:
  타임스탬프 있음?  Y / N
  모듈 prefix 있음? Y / N
  severity 있음?    Y / N
  기타 필드:

# → 5.5 pre-filter가 _l1sw.txt에서 grep할 때 어떤 필드를 기준으로
#   매칭해야 하는지 결정하는 데 필요

---

B6. .sdm → _l1sw.txt 변환 시 제거되는 정보는?
  1. 바이너리 데이터만 제거
  2. 바이너리 + 특정 모듈 전체 제거 (어떤 모듈:              )
  3. 바이너리 + 반복 heartbeat/polling 제거
  4. fragment JSON에 매칭 안 되는 라인 전부 제거
  5. 기타:

응답:

5.5 분석에 필요한 정보가 이 단계에서 잘릴 가능성은?
  1. 없음 — issue_type 관련 키워드는 모두 살아남음
  2. 가능성 있음 (어떤 경우:                      )
  3. 모르겠음 — 확인 필요
  4. 기타:

응답:

# → "2"이면: 5.5가 _l1sw.txt만 쓰면 안 되는 케이스가 존재
#   → scripts/ pre-filter를 raw log 대상 fallback으로 유지해야 함

═══════════════════════════════════════════════════
## C. 누적/학습/피드백 메커니즘
═══════════════════════════════════════════════════

C1. fragment JSON의 키워드/regex는 누가 업데이트하는가?
  1. 스킬 개발자가 수동 편집
  2. 분석 결과 기반 반자동 추가 (도구:              )
  3. 코드 릴리스마다 갱신
  4. 업데이트 된 적 없음 (초기 상태 그대로)
  5. 기타:

응답:

# → 1·4면: 5.5 누적 루프가 키워드 관리의 공식 채널이 됨
# → 2면: L1SW의 반자동 추가 메커니즘과 5.5 루프 간 연계 설계 필요

---

C2. "이 키워드가 빠져있다/regex가 넓다" 발견 시 피드백 경로는?
  1. 공식 경로 없음 — 비공식(구두/메신저) 요청
  2. 공식 경로 있음 (방법:                      )
  3. 사용자가 직접 fragment JSON 수정 가능
  4. 기타:

응답:

# → 1이면: 5.5의 keywords.yaml 누적 루프가 사실상 유일한 공식 관리 체계
# → 3이면: 사용자가 두 곳(fragment JSON + keywords.yaml) 모두 관리해야 하므로
#          동기화 설계 필요

---

C3. ★ L1SW 스킬 강화(skill reinforcement) 메커니즘이 있는가?
  1. 없음 — 분석할 때마다 동일 스킬 사용
  2. 있음 — 모듈 단위로 보강됨 (설명:                      )
  3. 있음 — 이슈/증상 패턴 단위로 보강됨 (설명:                      )
  4. 있음 — 보강 축 불명확 (설명:                      )
  5. 기타:

응답:

# ★ 핵심 질문 — skills_seed/ 겹침 여부 결정
# → 1이면: skills_seed/가 누적 지식의 유일한 축적처
# → 2이면: L1SW(모듈 축) + skills_seed(이슈 축) 병행 — 직교하므로 충돌 없음
# → 3이면: skills_seed/와 직접 겹침 가능 → 역할 재정의 또는 통합 필요

---

C4. 지금까지 L1SW 분석 결과물(HTML) 축적 현황은?
  1. 체계적 보관 — 한 폴더/시스템에 모아둠 (대략     건)
  2. 개별 저장 — 분석할 때마다 따로 저장 (대략     건)
  3. 보관하지 않음 — 분석 후 폐기
  4. 기타:

응답:

# → 1이면: 기존 HTML 리포트에서 키워드 후보 역추출 가능성 있음
# → 2·3이면: 축적은 5.5 cases/에서 처음 시작하는 셈

═══════════════════════════════════════════════════
## 응답 완료 후
═══════════════════════════════════════════════════

이 파일을 저장한 뒤 새 세션에 전달한다.
함께 전달할 파일: 이 패키지의 HANDOFF.md

핵심 분기 요약:
  B3 + B4 → keywords.yaml SSOT 범위 + 2차 grep 필요성
  C3      → skills_seed/ 역할 확정 (단독 vs 보완 vs 통합)
  A2 + B6 → _l1sw.txt만 쓸 것인지, raw log fallback 필요한지
