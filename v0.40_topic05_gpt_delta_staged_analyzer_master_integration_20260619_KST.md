# GPT Delta Draft — v0.40 topic05 Master Integration (staged-code-analyzer)

**작성:** Claude (비정규 흐름 — 사용자 S2 진행 지시에 따름)  
**기준:** Master v0.39.md  
**대상:** topic05 설계 결과 → Master 5.2, 00_overview.md, 5.0 Framework 반영  
**상태:** **S2 초안** — 사용자 검토 후 S4(Claude Review) 진행

---

## 1. 변경 범위 및 근거

### 1.1 변경 배경

topic05 (staged-code-analyzer 설계)에서 다음 핵심 결정이 확정되었다:

| 항목 | 결정 | 근거 |
|------|------|------|
| **주요 산출물** | Call graph + MSC (MSC-primary) | 대용량 폴더 30분+ 분석 시 문맥 초과 위험; MSC는 call flow 핵심만 가시화 |
| **HLD 전체 생성** | 제외 | MSC-primary로 전면 수정; 간단 모듈소개만 함께 제공 |
| **Track 구조** | Track A (스킬 실행) + Track B (bash 정적 추출) | §5.0.9 Skill Layer 우선; py_package는 후순위 가속기로 재배치 |
| **대용량 처리** | staged-code-analyzer (단계별 분석) | §5.0.10 Staged Analysis 메커니즘 도입; Global CNF Carry (CNF 1회 분석 → 전 Stage 공유) |
| **Track B 구현** | bash 프롬프트 기반 (CLI 불가) | §5.0.5 "Prefer Existing Environment"; Python 패키지 미사용 |

**결과:**
- Master 5.2 설명: "HLD 문서 초안 생성" → "Call graph + MSC + 간단 모듈소개"로 변경
- prompt/00_overview.md: Track A/B/py_package 3-way 관계 명시
- Master 5.0: staged-code-review/staged-code-analyzer 교차참조 추가

---

## 2. Master Roadmap v0.39 → v0.40 변경안

### 2.1 §5.2 Code Analyzer (라인 157-179 전체 재작성)

#### **[BEFORE] 현재 Master v0.39**

```markdown
### 5.2 Code Analyzer
```text
코드 루트 경로 + 분석 대상 지정
  ↓
코드 구조 분석 (함수/클래스/호출 관계/Branch 조건)
  ↓
Call Flow 추출
  ↓
HLD 업데이트 항목 목록 생성
  ↓
HLD 문서 초안 생성
  ↓
(선택) Confluence 업로드 또는 로컬 저장
```
Code Analyzer는 HLD 없이 코드만 존재하는 기존 구현을 분석하여 HLD(또는 구현문서)를 문서화하는 Workflow이다.
개별 담당자가 자신의 담당 코드를 대상으로 직접 실행하며, 분석 결과로 HLD 업데이트 항목 목록과 HLD 문서 초안을 생성한다.
5.4 HLD ↔ Code Consistency Check와의 역할 구분: 5.2는 HLD가 없는 상태에서 코드로부터 HLD를 새로 만들고, 5.4는 HLD가 이미 있는 상태에서 코드와의 불일치를 찾는다.
**Expected Benefit**
- HLD 미작성 코드의 문서화 부채 해소; 신규 인원 온보딩 지원; 코드 리뷰 품질 향상; HLD 작성 시간 단축; Call Flow 가시화; 구현 의도 복원
**Precondition**
- 없음; 독립 착수 가능; 분석 대상 코드 경로 및 언어/확장자 지정 필요; HLD 출력 템플릿 정의 필요
**Storage / Reuse**
- HLD 업데이트 항목 목록; HLD 문서 초안; Call Flow MSC(PlantUML); 함수/클래스 구조 분석 결과 JSON; 5.4 Consistency Check 입력으로 재사용 가능; 5.5 RCA Knowledge Graph의 API 연결 정보로 재사용 가능; 5.6 Onboarding Knowledge Pack의 HLD 링크로 활용 가능
```

---

#### **[AFTER] 신규 Master v0.40 제안안**

```markdown
### 5.2 Code Analyzer

#### 5.2.1 개요 (Overview)

Code Analyzer는 HLD 없이 코드만 존재하는 기존 구현을 분석하여 **Call Flow의 가시화 (Call Graph + MSC) 및 간단한 모듈 설명 문서**를 생성하는 Workflow이다.

특히 대용량 폴더(30분 이상의 분석 시간)를 처리할 때, §5.0.10 Staged Analysis 메커니즘을 사용하여 분석을 단계별로 나누고, Global CNF Carry를 통해 CNF 파일 분석 결과를 모든 단계에서 재사용한다.

개별 담당자가 자신의 담당 코드를 대상으로 직접 실행하며, 분석 결과로 다음을 생성한다:
- **Call Flow MSC (PlantUML Sequence Diagram)** — 주요 IPC 호출 흐름 시각화 (MSC-primary)
- **간단한 모듈 설명** — 각 모듈의 역할, 책임, 주요 함수/API
- **구조 분석 메타데이터 (JSON)** — 함수/클래스, 호출 관계, Branch 조건, IPC REQ/CNF 분할

5.4 HLD ↔ Code Consistency Check와의 역할 구분: 
- 5.2는 코드로부터 **Call Flow 가시화 (MSC) + 모듈 설명**을 새로 생성하는 데 중점을 둔다.
- 5.4는 HLD가 이미 있는 상태에서 코드와의 **불일치를 찾는 데** 중점을 둔다.

#### 5.2.2 실행 옵션 (Execution Tracks)

Code Analyzer는 두 가지 Track으로 운영된다:

##### **Track A: Skill-based Staged Analysis (권장 — 메인 경로)**

```text
코드 루트 경로 + 분석 대상 폴더 + 단계 범위 지정
  ↓
[Phase 0] 전체 폴더 인벤토리 (함수/클래스/호출 계수)
  ↓
[Phase 1..N] 단계별 분석 (각 단계별 REQ-site 파일 그룹 분석)
  │  각 단계:
  │  - 대상 폴더 코드 구조 분석 (함수/클래스/호출 관계/Branch 조건)
  │  - Call Flow 추출 (IPC REQ 사이트 중심)
  │  - Global CNF Carry 체크포인트 적용 (CNF 처리 파일은 1회만 분석, 이전 Stage 결과 재사용)
  │
[Phase F] 전 Stage 결과 통합
  ↓
Call Flow MSC (PlantUML) 생성
  ↓
간단한 모듈 설명 문서 + JSON 메타데이터
  ↓
(선택) Confluence 업로드 또는 로컬 저장
```

**특징:**
- `staged-code-analyzer` Skill으로 구현 (§5.0.9 Skill Layer)
- `5_2_code_analyzer_track_a_prompt.md`로 실행
- 대용량 폴더(30분+) 분석 시 문맥 초과 위험 방지
- Global CNF Carry: CNF 파일(IPC 응답)은 모든 Stage에서 동일하므로, 1회 분석 후 다음 Stage에서 재사용

##### **Track B: Static Extraction (선택적 — 사전 단계)**

```text
코드 루트 경로 지정
  ↓
Bash 프롬프트 기반 정적 구조 추출
  (find / wc / ctags / grep 등 기존 환경 활용)
  ↓
구조 JSON 생성 (module layout, function inventory, include hierarchy)
  ↓
Track A 입력으로 활용 (선택적)
```

**특징:**
- §5.0.5 "Prefer Existing Environment" 원칙 준수
- Python 패키지 불필요; bash 프롬프트 기반 실행
- `5_2_code_analyzer_track_b_prompt.md`로 실행
- Track A 진입 전 "코드 구조 전체 파악" 필요 시 사용 (선택적)
- 시간 효율: 구조 파악만 ~5분, HLD 산문은 절감 안 됨

##### **기존 py_package 프롬프트 (참고용)**

```text
5_2_code_analyzer_py_package_prompt.md
```

기존 Python 패키지 기반 프롬프트는 historical record로 보존된다. 향후 Track A/B와의 성능 비교 또는 특수 목적(예: 자동 HLD 초안 생성)에서 사용 가능한 대체 경로이다.

#### 5.2.3 Global CNF Carry 메커니즘

L1 모뎀 구현에서 IPC REQ/CNF는 다음과 같은 구조를 가진다:

- **IPC REQ 사이트:** 여러 파일에서 발생 (브랜치별로 다름)
- **IPC CNF 처리:** 특정 1개 파일에서만 처리 (모든 REQ의 응답 처리)

따라서 대용량 폴더를 Stage로 분할할 때:
1. **[Phase 1]** CNF 파일 포함 Stage에서 CNF 처리 로직을 완전히 분석 → `review_progress.md` 체크포인트에 저장
2. **[Phase 2..N]** 이후 Stage에서는 CNF 분석 결과를 재사용 (CNF 파일 재분석 불필요)
3. **[Phase F]** 모든 Stage의 REQ + 공유 CNF 결과 통합 → 최종 Call Flow MSC 생성

이를 "Global CNF Carry"라고 부른다.

#### 5.2.4 Expected Benefit

- **Call Flow 가시화:** 대용량 코드에서도 IPC 호출 흐름 시각화 (MSC)
- **구현 의도 복원:** 간단한 모듈 설명으로 신규 인원 빠른 이해
- **문맥 초과 방지:** Staged Analysis로 30분+ 분석도 완료
- **코드 리뷰 품질:** Call Flow 이해 → 리뷰 포인트 명확화
- **5.4/5.5/5.6 입력 기반:** 생성된 MSC 및 구조 메타데이터를 후속 항목에 재사용

#### 5.2.5 Precondition

- 분석 대상 코드 경로 및 폴더 단계 범위 지정 필요
- Track A 사용 시: Claude Code 또는 Roo Code 사용 가능
- Track B 사용 시: bash 환경 + find/wc/ctags/grep 기본 도구

#### 5.2.6 Storage / Reuse

- Call Flow MSC (PlantUML) — 5.4/5.5 입력으로 재사용 가능
- 간단한 모듈 설명 문서 — 신규 인원 온보딩, 코드 리뷰 가이드
- 구조 분석 메타데이터 (JSON) — 5.4 Consistency Check, 5.5 RCA Knowledge Graph 입력
- review_progress.md (Stage 체크포인트) — 재개 또는 다시 실행 시 Skip 지점 명시
```

---

### 2.2 Master v0.39 필요 추가 섹션

Master에 5.0 Common Framework와 staged 메커니즘에 대한 교차참조가 부족하다. 아래 섹션을 추가하거나 강화한다:

#### **5.0 Common Automation Framework 섹션 (기존 또는 신규 추가)**

현재 Master v0.39에는 5.0 프레임워크에 대한 명시적 설명이 없다. 다음을 추가 권장:

```markdown
## 5.0 Common Automation Framework

모든 5.x 자동화 항목은 다음 공통 원칙을 따른다:

### 5.0.5 Prefer Existing Environment
새로운 도구나 패키지를 추가하기보다, 기존 bash/grep/find 등 환경 도구를 최대한 활용한다.
예: Track B는 Python CLI가 아닌 bash 기반 정적 추출 사용.

### 5.0.9 Skill Layer vs Workflow Layer
- **Skill Layer:** 재사용 가능한 분석 로직 (staged-code-analyzer 등) — Roo Code/Claude Code로 실행
- **Workflow Layer:** prompt/ 폴더의 실행 프롬프트 — 팀원이 직접 입력하여 실행

두 Layer는 명확히 분리하여 관리한다. 스킬은 ZIP 패키지로 배포; 프롬프트는 L1a/prompt/에서 버전 관리.

### 5.0.10 Staged Analysis for Large Codebases
대용량 폴더(30분 이상 분석)는 다음 구조로 나눈다:
1. **Phase 0:** 전체 인벤토리 (빠른 카운트)
2. **Phase 1..N:** 단계별 REQ-site 분석 (각 Stage는 독립적 문맥)
3. **Checkpoint (review_progress.md):** 각 Stage 완료 후 진행 상태 저장
4. **Global Carry Mechanism:** Stage별 분석 결과 중 공유 요소(예: CNF 파일 처리)는 1회만 분석하고 이후 Stage에서 재사용
5. **Phase F:** 전 Stage 결과 통합 및 최종 산출물 생성

이를 통해 문맥 초과 위험을 방지하고, 재개/재실행 시 효율성을 높인다.
```

---

## 3. prompt/00_overview.md 변경안

### 3.1 "항목 목록" 섹션 확장 (라인 49-58)

#### **[BEFORE]**

```markdown
| 파일 | 항목 | 핵심 목적 |
|------|------|----------|
| `5_0_common_automation_framework.md` | Common Automation Framework | 5.1~5.6 공통 철학, Layer, Artifact, Skill Loading 기준. ARTIFACTS_BASE_DIR 기준 경로 정의 포함 |
| `5_1_jira_feedback_loop_py_package_prompt.md` | Jira Feedback Loop | Critical Defect → P4 Shelve → Jira 등록 → RCA → Prevent Rule |
| `5_2_code_analyzer_py_package_prompt.md` | Code Analyzer | HLD 없는 기존 코드 분석 → HLD 업데이트 항목 + HLD 초안 생성 |
| ... (이하 생략)
```

#### **[AFTER]**

```markdown
| 파일 | 항목 | 핵심 목적 |
|------|------|----------|
| `5_0_common_automation_framework.md` | Common Automation Framework | 5.1~5.6 공통 철학, Layer, Artifact, Skill Loading 기준. ARTIFACTS_BASE_DIR 기준 경로 정의 포함. Staged Analysis (§5.0.10), Prefer Existing Environment (§5.0.5) 포함 |
| `5_1_jira_feedback_loop_py_package_prompt.md` | Jira Feedback Loop | Critical Defect → P4 Shelve → Jira 등록 → RCA → Prevent Rule |
| `5_2_code_analyzer_track_a_prompt.md` | **Code Analyzer — Track A (Skill-based, 권장)** | **대용량 폴더 단계별 분석 → Call Graph/MSC 생성 (MSC-primary) + 간단 모듈소개** |
| `5_2_code_analyzer_track_b_prompt.md` | **Code Analyzer — Track B (Static Extraction, 선택적)** | **bash 정적 구조 추출 → JSON 메타데이터. Track A 진입 전 사전 단계** |
| `5_2_code_analyzer_py_package_prompt.md` | Code Analyzer — Legacy (참고용) | HLD 없는 기존 코드 분석 → HLD 초안 생성 (과거 방식, 대용량 미지원) |
| ... (이하 생략)
```

---

### 3.2 "항목 간 의존 관계" 섹션 보강 (라인 62-72)

#### **[BEFORE]**

```markdown
```text
5.1    ────────────────────────────────────────────── 독립
5.2    ────────────────────────────────────────────── 독립
5.3-pre ────────────────────────────────────────────── 독립 (5.3의 선행 실행 필요)
5.3    ── (5.3-pre draft 필요) ─────────────────────── 의존
5.4    ── (5.2 완료 권장) ──────────────────────────── 부분 의존
5.5    ── (5.2 완료 필요) ──────────────────────────── 의존
5.6    ── (5.2 + 5.5 완료 필요) ───────────────────── 강한 의존
```
```

#### **[AFTER]**

```markdown
```text
5.1    ────────────────────────────────────────────── 독립

5.2    ┌─ Track A (권장, 메인 경로)
       │  └─ staged-code-analyzer Skill 사용
       │
       └─ Track B (선택적, 사전 단계)
          └─ bash 정적 추출
       
       또는 Legacy py_package (과거 방식, 대용량 미지원)
       ────────────────────────────────────────────── 독립

5.3-pre ────────────────────────────────────────────── 독립 (5.3의 선행 실행 필요)

5.3    ── (5.3-pre draft 필요) ─────────────────────── 의존

5.4    ── (5.2 Track A 또는 Legacy 완료 권장) ─────── 부분 의존
       (Track B 단독으로는 5.4 진입 부족 — Track A 먼저 실행)

5.5    ── (5.2 Track A 또는 Legacy 완료 필요) ─────── 의존

5.6    ── (5.2 Track A 또는 Legacy + 5.5 완료 필요) ─ 강한 의존
```
```

---

### 3.3 5.2 Track A/B 선택 가이드 추가 (신규 섹션)

라인 90 "운영 원칙" 이전에 아래 섹션 추가:

```markdown
## 5.2 Code Analyzer — Track 선택 가이드

### Track A 사용 (권장 — 메인 경로)

**조건:**
- 대용량 폴더 분석 (30분 이상 예상)
- Call Flow MSC 시각화 필요
- 신규 인원 빠른 이해 (간단 모듈소개)

**준비물:**
- Claude Code 또는 Roo Code
- staged-code-analyzer Skill 로드 필요 (07_skills_v1_3.zip)
- 분석 대상 폴더 + 단계 범위 사전 지정

**실행:**
- `5_2_code_analyzer_track_a_prompt.md`를 Claude Code/Roo Code에 입력
- §5 Section 9 "실행 프롬프트" 직접 사용

### Track B 사용 (선택적 — 사전 단계)

**조건:**
- Track A 진행 전 "전체 코드 구조 파악" 필요
- bash 환경에서 빠른 인벤토리 원할 때 (5~10분)

**준비물:**
- bash 환경 + find/wc/ctags/grep 기본 도구
- 분석 대상 폴더

**실행:**
- `5_2_code_analyzer_track_b_prompt.md`를 Claude Code/Roo Code에 입력
- 생성된 JSON 메타데이터를 Track A의 "분석 대상 폴더 지정" 단계에서 활용 (선택적)

### Legacy py_package 사용

**조건:**
- 과거 HLD 초안 생성 방식 선호
- 대용량 미지원 (30분 이내 분석)
- 자동 HLD 산문 생성 필요

**준비물:**
- Python 패키지 환경 (구현 예정)

**실행:**
- `5_2_code_analyzer_py_package_prompt.md` 참고
- 현재는 프롬프트 형태; 추후 Python 패키지 구현 계획

### 권장 순서

1. **1차:** Track B로 구조 파악 (~5분)
2. **2차:** Track A 진입, staged-code-analyzer로 단계별 분석 (~20-30분, 폴더 크기에 따라)
3. **결과:** Call Flow MSC + 간단 모듈소개 + JSON 메타데이터

Track B 스킵 가능 (Track A 단독 사용 가능).
```

---

## 4. 검토 포인트 (S4 진행 전 사용자 확인)

아래 항목을 **검토 후 ACCEPT/MODIFY/REJECT** 결정 요청:

| # | 항목 | 현재 제안 | 검토 필요 항목 |
|---|------|----------|---------------|
| 1 | Master 5.2 구조 | MSC-primary로 전면 개편 | "간단 모듈소개"의 분량/포맷 정의 필요한가? |
| 2 | Track A/B 명칭 | "Track A (Skill-based)" / "Track B (Static)" | 명칭이 직관적인가? |
| 3 | Global CNF Carry 설명 | 5.2.3 별도 섹션 추가 | L1-specific 용어("IPC REQ/CNF") 사용이 적절한가? |
| 4 | Legacy py_package | historical record로 보존, 별도 계열로 명시 | py_package 향후 제거 예정인가, 아니면 대체 경로로 유지인가? |
| 5 | Master 5.0 섹션 추가 | 5.0.5 / 5.0.9 / 5.0.10 신규 추가 | Master에 5.0 프레임워크 섹션이 필요한가? (현재 없음) |
| 6 | 버전 번호 | v0.39 → v0.40 | 버전 번호 증가가 적절한가? (topic05는 Master 큰 변경이므로 v0.40 제안) |

---

## 5. S4 진행 예정

사용자 검토 완료 후:

1. **S4:** Claude Review Delta 작성
   - 본 초안의 명확성, 일관성, 완전성 검토
   - 추가 영향 범위(예: 5.4 설명의 변경 필요 여부) 점검

2. **S5:** 사용자 수용/기각 판단

3. **S6:** Claude Merge Delta 작성
   - Master v0.39 → v0.40 실제 패치 생성

4. **S7:** 사용자 최종 승인

5. **S8/S9:** New Master 생성 + 패키지 재생성

---

## 부록: 참고 파일

- `review_logs/v0.39_topic05_decision_log_20260618_KST.md` — topic05 설계 경과 및 확정 항목
- `prompt/5_2_code_analyzer_track_a_prompt.md` — Track A 실행 프롬프트 (완료)
- `prompt/5_2_code_analyzer_track_b_prompt.md` — Track B 실행 프롬프트 (완료)
- `07_skills_v1_3.zip` — staged-code-analyzer + staged-code-review Skill (완료)

---

**상태:** ✅ S2 초안 완료 — 사용자 검토 및 피드백 대기
