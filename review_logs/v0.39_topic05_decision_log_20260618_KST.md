# Decision Log 확정 — topic05 (비정규 흐름: S1 생략)

- 확정: 2026-06-18 KST
- Base: master/L1_AI_Automation_Roadmap_v0.39.md (변경 없음)
- Result: 변경 없음 — Master 버전업 없음 (Master 반영은 추후 GPT S1 논의 결과에 따름)

---

## ⚠ 비정규 흐름 명시

본 topic은 표준 S0–S9 순서를 따르지 않았다.

```text
표준 흐름:   S0 → S1(GPT 논의) → S2(GPT Delta) → S3 → S4(Claude Review) → ...
실제 흐름:   S0 → [S1 생략] → Claude와 직접 설계/구현 (본 로그가 그 결과 기록) → S1 예정
```

- S1(GPT와 주제별 논의), S2(GPT Delta 작성)을 생략하고, 사용자가 Claude와 직접 대화하며
  설계/구현/패키징까지 진행했다.
- 본 Decision Log는 일반적인 "GPT Delta 검토 확정" 기록이 아니라, **S1 생략 사실과 그 동안
  Claude와 직접 합의한 항목을 사후 기록**하는 용도다.
- Next Step은 새 topic이 아니라 **이 topic05를 유지한 채 S1(GPT 논의)로 이어간다.** GPT S1
  논의 결과 Master 반영이 필요해지면 그 시점에 S2 이후 표준 흐름으로 복귀한다.

---

## 진행 경과 (Claude와 직접 합의한 항목)

| # | 항목 | Claude 제안 | 사용자 판단 | 반영 결과 |
|---|------|------------|------------|---------|
| 1 | 5.2 Code Analyzer를 폴더 전체(30분 소요)에 적용 가능한가 | 직접 적용 불가, `staged-code-review`의 단계분할 구조는 재사용 가능 | ACCEPT | `staged-code-analyzer` 신규 스킬 설계 착수 |
| 2 | 스킬 vs Python 패키지 — 어느 쪽을 5.2 1차로 할지 | §5.0.9/§5.0.10 근거로 Skill Layer(스킬) 우선, 기존 py_package 프롬프트는 후순위 가속기로 재배치 | ACCEPT | Track 구조(A=스킬 실행, 후일의 파서=가속기) 채택 |
| 3 | Track B(파서)의 토큰 효율 가치 범위 | 1차 답변: 구조 추출만 절감, HLD 산문은 절감 안 됨 | 정정 — "5.2 주목적은 call 그래프, 결과물은 간단 모듈소개+메인 MSC" | 풀 HLD 생성을 5.2 범위에서 제외, **MSC-primary**로 설계 전면 수정 |
| 4 | IPC REQ/CNF 구조 가정 | 1차 가정(async/런타임 dispatch 일반론)이 L1 실제와 불일치 | 정정 — "REQ는 블록별로 다르지만 CNF 처리 파일은 하나" | **Global CNF Carry** 메커니즘 도입 (CNF 1회 분석 → 체크포인트로 전 Stage 공유) |
| 5 | Track B 실행 방식 | CLI/Python 패키지가 아닌 프롬프트 기반 가능 여부 확인 요청 | "CLI 아닌 프롬프트로" 확정 | Track B를 bash 프롬프트 추출(find/wc/ctags/grep)로 구현, Python 패키지 미사용 (§5.0.5) |
| 6 | 3-file(`py_package` / `track_a` / `track_b`) 명칭 정합성 | 점검 중 "Track B" 명칭이 두 가지 다른 대상(구 py_package안 vs 신규 bash 추출)에 중복 사용된 것 발견 | ACCEPT (정정 지시) | 두 프롬프트 §0을 동일 비교표로 통일, "Track" 명칭은 신규 페어 전용으로 한정. py_package는 별도 계열로 명시 |
| 7 | L1a 통합 방식 | 정책(Skill Layer)과 워크플로우(prompt)를 같은 곳에 섞지 말고 분리 제안 | ACCEPT | `L1a/prompt/`에 Track A/B 프롬프트 2개 추가, `07_skills_v1_3.zip`에 스킬 2개 + Roo rule 2개 추가 — 별도 패키지로 전달 |
| 8 | 본 topic 기록 방식 | 사후 topic05 기록 제안, 처리 옵션 3가지 제시 | "topic05로 사후 기록, S1 생략 명시, GPT S1에서 이어서 논의" | 본 Decision Log 작성 + `last_status.md` topic05로 갱신 |

---

## 산출물 (전달 완료 — 사용자 로컬 병합 필요)

본 세션에서 만든 산출물은 L1a 저장소에 Claude가 직접 반영하지 않았다. 아래 ZIP으로 전달했으며,
사용자가 로컬 L1a clone에 직접 병합해야 한다.

| 파일/위치 | 내용 | 전달 방식 |
|------|------|---------|
| `.claude/skills/staged-code-review/SKILL.md` | 대용량 폴더 단계별 코드 리뷰 (결함 탐지) | `07_skills_v1_3.zip` |
| `.claude/skills/staged-code-analyzer/SKILL.md` | 대용량 폴더 단계별 call graph/MSC 생성 (Global CNF Carry 포함) | `07_skills_v1_3.zip` |
| `.roo/rules/08-staged-code-review.md` | 위 스킬의 Roo 1:1 rule | `07_skills_v1_3.zip` |
| `.roo/rules/09-staged-code-analyzer.md` | 위 스킬의 Roo 1:1 rule | `07_skills_v1_3.zip` |
| `L1a/prompt/5_2_code_analyzer_track_a_prompt.md` | staged-code-analyzer 스킬 호출 실행 프롬프트 (메인 경로) | `L1a_prompt_5_2_addition.zip` |
| `L1a/prompt/5_2_code_analyzer_track_b_prompt.md` | bash 정적 구조 추출 실행 프롬프트 (선택적 선행 단계) | `L1a_prompt_5_2_addition.zip` |

기존 `L1a/prompt/5_2_code_analyzer_py_package_prompt.md`는 수정/삭제하지 않고 historical record로 보존했다.

---

## 미반영 (S1에서 다룰 항목)

다음은 Claude와 직접 설계한 결과물일 뿐, **Master Roadmap 5.2 항목 정의 자체는 아직 갱신되지
않았다.** GPT S1 논의에서 다룰 것:

- Master `5.2 Code Analyzer` 섹션을 "HLD 초안 생성" 중심 서술에서 "call graph + MSC + 간단
  모듈소개" 중심으로 갱신할지 여부
- Track A(스킬) / Track B(bash 추출) / 기존 py_package안의 3-way 관계를 Master/00_overview.md
  의존 관계 표에 어떻게 반영할지
- `staged-code-review`/`staged-code-analyzer`를 5.0 공통 프레임워크의 어느 절(§5.0.x)에
  공식 인용으로 추가할지
- Master 버전업(v0.40) 필요 여부
