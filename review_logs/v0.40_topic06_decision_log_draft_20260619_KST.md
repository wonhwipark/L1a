# Decision Log Draft — v0.40 topic06 5.5 Large Log Pre-filter Step 1

**작성:** Claude  
**시간:** 2026-06-19 KST  
**상태:** 초안 — S5에서 사용자 선택 후 확정

---

## 1. Topic 개요

| 항목 | 내용 |
|------|------|
| Topic | topic06 — 5.5 RCA Knowledge Graph Large Log Pre-filter Step 1 |
| Base | `master/L1_AI_Automation_Roadmap_v0.40.md` |
| S2 Delta | `delta/v0.40_topic06_gpt_delta_5_5_large_log_prefilter_step1_20260619_KST.md` |
| S4 Review | `review_logs/v0.40_topic06_claude_review_delta_20260619_KST.md` |
| 핵심 목적 | 100MB~1GB 대용량 로그를 AI Context에 직접 입력하지 않고, 문제 유형별 signal 추출과 RCA Seed 축적 구조를 정의 |

---

## 2. 필수 결정 사항

### 2.1 Prompt 반영 범위

**배경:**  
S2 Delta에는 Step 1 pre-filter뿐 아니라 500회 이상 반복 분석을 위한 파일 기반 Knowledge Graph 운영안도 포함되어 있다.

**선택지:**

#### 선택 A) Step 1 pre-filter와 RCA Markdown만 반영

```text
결정:
- prompt/5_5에는 large log path, issue type, pre-filter rule, signal output, RCA Markdown template만 추가
- 500회 이상 운영안은 Delta 기록으로만 유지

장점:
- 반영 범위가 작고 명확함
- prompt가 과도하게 길어지지 않음

단점:
- 장기 Knowledge Graph 운영 방향이 prompt에 충분히 남지 않음
```

#### 선택 B) Step 1 + 파일 기반 KG 운영안 요약까지 반영 — 권장

```text
결정:
- Step 1 pre-filter와 RCA Markdown template을 prompt에 반영
- rca_kg/cases, signals, skills_seed, indexes, schema 구조를 요약 반영
- 상세 case YAML 예시는 Delta에 유지하고 prompt에는 요약만 포함

장점:
- 5.5 Knowledge Graph의 장기 방향이 명확해짐
- 500회 이상 반복 분석을 위한 저장/검색 방향이 prompt에 반영됨

단점:
- prompt가 다소 길어짐
```

#### 선택 C) Step 1은 prompt에 반영, 파일 기반 KG 운영안은 별도 문서 후보로 분리

```text
결정:
- prompt/5_5에는 Step 1만 반영
- 파일 기반 KG 운영안은 향후 rca_kg/README 또는 별도 prompt로 분리

장점:
- 구현 프롬프트와 운영 설계를 분리 가능

단점:
- 추가 산출물 관리 필요
```

**사용자 선택:** A / B / C ?

---

### 2.2 issue_type 표준명

**배경:**  
현재 논의에서는 업무 표현과 저장용 식별자가 섞여 있다.

```text
rach failure / rach_failure
scgFailure / scg_failure
tx abnormal / tx_abnormal
L2 max retransmission / l2_retx / l2_max_retransmission
crash
```

**선택지:**

#### 선택 A) snake_case 저장 표준 사용 — 권장

```text
결정:
- 저장용 issue_type은 snake_case로 통일
- 표시명은 별도 display_name으로 유지 가능

표준 issue_type:
- rach_failure
- scg_failure
- tx_abnormal
- l2_max_retransmission
- crash
```

#### 선택 B) 현재 업무 표현 유지

```text
결정:
- 사람이 쓰는 표현을 그대로 issue_type으로 사용

예:
- rach failure
- scgFailure
- tx abnormal
- L2 max retransmission
- crash
```

**사용자 선택:** A / B ?

---

### 2.3 Master §5.5 반영 여부

**배경:**  
Delta는 `master/L1_AI_Automation_Roadmap_v0.40.md` §5.5에는 요약만, `prompt/5_5_rca_knowledge_graph_py_package_prompt.md`에는 상세를 반영하는 방식을 제안한다.

**선택지:**

#### 선택 A) Master 요약 + prompt 상세 반영 — 권장

```text
결정:
- Master §5.5에는 대용량 로그 pre-filter와 RCA Seed 축적 방향만 요약 반영
- prompt/5_5에는 입력/출력/구현 요구사항/RCA template/검증 기준 상세 반영

장점:
- Master는 방향성을 유지하고 prompt는 실행성을 담당
- 기존 문서 구조와 일관됨
```

#### 선택 B) prompt만 반영, Master는 유지

```text
결정:
- Master v0.40은 수정하지 않음
- prompt/5_5에만 Step 1 상세 반영

장점:
- Master 변경 최소화

단점:
- Master §5.5만 보면 대용량 로그 처리 방향이 드러나지 않음
```

#### 선택 C) 이번 topic06에서는 Delta만 유지

```text
결정:
- 실제 master/prompt 반영은 보류
- Delta를 설계 기록으로 유지

장점:
- 추가 검토 시간 확보

단점:
- 실행 프롬프트에는 아직 반영되지 않음
```

**사용자 선택:** A / B / C ?

---

## 3. 선택 사항

### 3.1 Windows 환경 고려 문구 추가

**선택 A) 추가 — 권장**

```text
구현자는 현재 환경을 먼저 확인하고, PowerShell/Python/Git Bash 중 사용 가능한 방식으로 pre-filter를 구현한다.
grep/awk만을 전제로 하지 않는다.
```

**선택 B) 추가하지 않음**

```text
환경 선택은 구현자에게 맡기고 prompt에는 별도 명시하지 않음.
```

**사용자 선택:** A / B ?

---

### 3.2 Schema validation 도입 시점

**선택 A) Step 1에서는 template만, Step 2에서 validation 추가 — 권장**

```text
Step 1: RCA Markdown + case YAML template
Step 2: schema validation 추가
```

**선택 B) Step 1부터 schema validation 요구사항 포함**

```text
초기부터 schema validation을 구현 요구사항에 포함
```

**사용자 선택:** A / B ?

---

## 4. 권장 선택 요약

| 항목 | 권장 선택 |
|------|-----------|
| 2.1 Prompt 반영 범위 | B |
| 2.2 issue_type 표준명 | A |
| 2.3 Master §5.5 반영 여부 | A |
| 3.1 Windows 환경 고려 문구 | A |
| 3.2 Schema validation 도입 시점 | A |

---

## 5. S5 사용자 판단 요청

아래 형식으로 선택하면 S6 Merge Delta를 작성할 수 있다.

```text
2.1: B
2.2: A
2.3: A
3.1: A
3.2: A
```

권장 선택은 `B / A / A / A / A` 이다.
