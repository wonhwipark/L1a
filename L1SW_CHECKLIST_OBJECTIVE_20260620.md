# L1SW 확인 — 객관식 응답지 (2026-06-20)
# 사용법: 각 질문 아래 번호에 O 표시 또는 번호만 적기. 복수 가능 시 명시.
# 우선순위: A > B,C,E > D,F

═══════════════════════════════════════════════════
## A. 지식 누적 구조 (★최우선)
═══════════════════════════════════════════════════

A1. 지식 누적은 어디에, 어떤 형식으로 저장되는가?
  1. skill 파일(.md) 자체를 수정하는 방식
  2. 별도 데이터 파일(JSON/YAML/DB)에 쌓음
  3. manifest fragment JSON을 갱신
  4. 누적 메커니즘이 사실상 없음 (1회성 분석)
  5. 기타:

응답: 

A2. 누적 단위는 무엇인가?
  1. 모듈 기반 (예: [DSSM] 모듈 패턴이 쌓임)
  2. 이슈/증상 패턴 기반 (예: "msg2 timeout" 현상 단위)
  3. 사람이 수동으로 skill에 메모 추가
  4. 모듈 + 이슈 패턴 혼합
  5. 기타:

응답: 

A3. 누적된 지식이 다음 분석에 어떻게 재사용되는가?
  1. 자동으로 프롬프트/컨텍스트에 주입됨
  2. 분석가가 수동으로 참조
  3. 재사용 경로 없음 (저장만 됨)
  4. 기타:

응답: 

# → A2가 1·3이면: 5.5 skills_seed와 안 겹침 → 현 설계 유지
# → A2가 2·4이면: ⑥ 중복 위험 → skills_seed 역할 재정의 필요

═══════════════════════════════════════════════════
## B. issue_type 분류 (5.5 ① 영역)
═══════════════════════════════════════════════════

B1. 출력에 문제 유형 라벨(RACH/SCG 등)이 붙는가? (severity 배지 말고 '유형')
  1. 붙음 — issue_type에 준하는 유형 라벨 있음
  2. 안 붙음 — 모듈명만 있고 유형 분류 없음
  3. 부분적 — 일부 유형만 (어느 것:           )
  4. 기타:

응답: 

B2. manifest fragment JSON의 regex는 무엇을 매핑하는가?
  1. 모듈명만 매핑
  2. 모듈명 + 증상 키워드(예: PHY_TIMER_EXPIRY) 둘 다
  3. 증상 키워드 위주
  4. 기타:

응답: 

B3. -Modules로 거를 수 있는 모듈 목록 전체를 확인 가능한가?
  1. 가능 — 목록 확보함 (별도 첨부/메모)
  2. 일부만 확인됨
  3. 확인 불가
  4. 기타:

응답: 

═══════════════════════════════════════════════════
## C. 출력 형식과 구조화 (5.5 ② 영역)
═══════════════════════════════════════════════════

C1. HTML 외 기계가 읽을 출력(JSON/YAML/CSV) 옵션이 있는가?
  1. 없음 — HTML only
  2. 있음 — 형식:
  3. 기타:

응답: 

C2. HTML 안 라인번호/패턴 발생횟수/시간 정보가 파싱 가능한가?
  1. 파싱 가능 (구조적으로 들어있음)
  2. 사람이 읽는 텍스트로만 (파싱 어려움)
  3. 일부만 구조적
  4. 기타:

응답: 

C3. _l1sw.txt의 라인 번호는 원본 .sdm 기준으로 보존되는가?
  1. 보존됨 — 원본 라인번호 그대로
  2. 재번호됨 — _l1sw.txt 자체 기준으로 다시 매겨짐
  3. 라인번호 없음
  4. 기타:

응답: 

# → C3이 2·3이면: 5.5 log_patterns의 line_range를 원본이 아닌
#   _l1sw.txt 기준으로 재정의해야 함 (schema 영향)

═══════════════════════════════════════════════════
## D. crash 분석 경계 (L1SW 전담 확정 → 위임 범위만)
═══════════════════════════════════════════════════

D1. crash(dump 기반) 분석의 출력 형식은?
  1. 다른 issue_type과 같은 HTML
  2. 별도 형식 (무엇:           )
  3. 기타:

응답: 

D2. crash 결과를 5.5 case가 참조하려면 어떤 연결이 가능한가?
  1. 파일 경로로 연결 가능
  2. 식별자(ID)로 연결 가능
  3. 연결 수단 마땅치 않음
  4. 기타:

응답: 

# → D2가 3이면: taxonomy에서 crash 완전 제거
# → D2가 1·2면: taxonomy에 crash 'L1SW 참조용' 한 줄로 축소 유지

═══════════════════════════════════════════════════
## E. 외부 자산 연결 (5.5 ⑤ 영역)
═══════════════════════════════════════════════════

E1. Jira/HLD/TC/API 등 외부 자산 연결 필드·기능이 있는가?
  1. 전혀 없음
  2. 일부 있음 (무엇:           )
  3. 기타:

응답: 

# → 1이면: ⑤는 온전히 5.5 고유 가치 (1순위 유지)
# → 2이면: 5.5 related 필드와 정합 필요

═══════════════════════════════════════════════════
## F. 운영 / 실행 환경
═══════════════════════════════════════════════════

F1. parse.ps1과 분석 스킬의 실제 경로(.claude/ 하위)는?
  메모:

F2. _l1sw.txt는 분석 후 보존되는가?
  1. 보존됨
  2. 자동 삭제됨
  3. 옵션으로 선택 가능
  4. 기타:

응답: 

F3. 한 번 분석에 들어가는 _l1sw.txt 대략 크기/토큰은?
  1. 수백 KB 수준
  2. 수 MB 수준
  3. 10MB 이상
  4. 기타:

응답: 

# → F2가 2면: 5.5 입력 전에 _l1sw.txt를 따로 복사·보존하는 단계 필요
